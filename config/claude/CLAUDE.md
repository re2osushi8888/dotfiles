# Global Instructions

- Do not add `Co-Authored-By` trailers to git commit messages
- Always pin versions to exact values (e.g. `1.26.4`). Never use `latest`, `>=x.x`, `^x.x`, `~x.x`, or other range/floating specifiers
